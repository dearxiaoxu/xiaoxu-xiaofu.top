# xiaoxu-xiaofu.top 整改方案

> 版本：v1.0 · 2026-08-18
> 定位：让「我们的小站」从「自嗨记录」变成「有人看、看得完、看完想分享」的双人生活站

---

## 0. 一句话结论

**这站不缺功能，缺的是「让别人知道 → 愿意点开 → 愿意留下」的传播闭环。**
技术底子（零依赖 Node + SQLite + 管理后台 + 暗色模式 + 中国地图）超过 95% 个人情侣站，问题不在代码，在于：没有文章评论、没有分享图、没有订阅/收录、没有访问统计——**访客来了留不下，走了叫不回来，你也不知道谁来过。**

整改核心思路：**先打通「内容生产 → 分享出口 → 数据反馈」的小闭环，再谈增长。**

---

## 1. 现状快照（2026-08-18 实勘）

| 维度 | 现状 | 评价 |
|---|---|---|
| 页面 | 首页/关于/博客/文章/项目/相册/城市地图/联系 | ✅ 结构完整 |
| 内容后台 | 管理后台可视化编辑 + SQLite 存储 + 备份导入导出 | ✅ 很能打 |
| 留言板 | 联系页全局留言（带限频+蜜罐，SQLite 200 条上限） | ✅ 有，但缺文章级评论 |
| SEO 基础 | 每页都有 og:title/description/url、canonical、favicon | ✅ 基础扎实 |
| **og:image 分享图** | **全站缺失** | 🔴 微信/小红书分享无图=没人点 |
| **文章评论** | **无**（只有全局留言板） | 🔴 访客没处互动 |
| **RSS / sitemap** | **无** | 🟡 无法订阅、搜索引擎收录差 |
| **访问统计** | **零埋点** | 🟡 不知道谁在看、看什么 |
| 城市地图 | 中国地图 + 8 城边界数据（assets/boundaries/） | 🟢 杀手锏功能，但没变成分享武器 |
| 技术栈 | 原生 HTML/CSS/JS + 零依赖 Node（`node:sqlite`）+ 手写路由 | ✅ 可维护性极佳 |

---

## 2. 整改原则

1. **保持零依赖哲学**：所有新功能优先用 `node:` 内置模块 + 现有架构实现，不引框架
2. **小步快跑**：每个 P0 项半天到一天内能上线，先跑起来再优化
3. **先传播、后功能**：P0 全是「分享/互动/收录」相关，P2 才谈体验深化
4. **数据先行**：统计上线前，一切"优化"都是拍脑袋

---

## 3. 整改计划（按优先级）

### 🚩 P0 · 本周（止血 + 打开传播开关）

| # | 项目 | 为什么先做 | 工作量 | 验收标准 |
|---|---|---|---|---|
| 1 | 文章级评论 | 互动是留客第一要素 | 0.5 天 | 文章页能发/看评论，防刷 |
| 2 | og:image 分享图 | 社交分发无图=零点击 | 0.5 天 | 微信/小红书分享出卡片图 |
| 3 | sitemap + robots + 百度收录 | 白嫖搜索引擎长尾流量 | 1 小时 | sitemap.xml 可访问，百度能抓到 |
| 4 | 访问统计（自研轻量） | 没数据就没法优化 | 2 小时 | 后台能看到 PV/UV/热帖（前台零痕迹） |

#### ① 文章级评论（最高 ROI）

**现状**：`store.js` 只有 `messages` 表（id, name, text, time），绑在联系页；文章页 `post.html` 无评论。

**方案**：
- `store.js` 新增评论表，复用现有 SQLite（WAL 模式）：
  ```sql
  CREATE TABLE IF NOT EXISTS comments (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    post_id TEXT NOT NULL,      -- 文章 id
    name TEXT NOT NULL,
    text TEXT NOT NULL,
    time INTEGER NOT NULL
  );
  CREATE INDEX IF NOT EXISTS idx_comments_post ON comments(post_id);
  ```
- `server.js` 在 `handleAPI()` 加两个路由（完全照抄 `/api/messages` 的限频+蜜罐+长度校验逻辑）：
  - `GET /api/comments?postId=xx` → 返回该文章评论列表
  - `POST /api/comments` → `{postId, name, text}` 写入（沿用 200 条上限策略，按 postId 分组）
- `js/site.js` 在 post 渲染后追加评论区；`post.html` 加评论容器与表单（样式复用留言板 CSS）
- 后台 `/admin` 加「评论管理」Tab，可删除（复用 messages 的查看/删除 UI 模式）

**注意**：评论区要转义 HTML（现有 `escapeHtml` 复用），`name` 限 20 字、`text` 限 500 字，与留言板同策略。

#### ② og:image 分享图

**现状**：全站 `og:image` 缺失（实锤，grep 无结果）。

**方案（两档）**：
- **MVP（半天）**：用现有上传图做站点默认分享图，每页 head 加：
  ```html
  <meta property="og:image" content="https://www.xiaoxu-xiaofu.top/data/uploads/<已上传图>.png">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  ```
  微信抓取用 1200×630 比例最佳。后台「站点设置」里加一个「默认分享图」上传项（复用 `/api/upload`）。
- **进阶（1 天）**：动态分享卡 `GET /api/og-card?id=<文章ID>`，服务端用纯 JS 拼 SVG（标题+作者+日期+暖橙主题色），前端 `js/site.js` 渲染 post 时动态注入 `<meta og:image>`。SVG 可被多数抓取器识别；若微信不认 SVG，则配合 P1 的「海报生成器」产出 PNG。

#### ③ sitemap + robots + 百度收录

**方案**：
- `server.js` 静态路由加两个伪文件（或直接放静态目录）：
  - `/robots.txt`：允许全部 + `Sitemap:` 声明
  - `/sitemap.xml`：从 SQLite 读文章/项目列表动态生成（`<lastmod>` 用文章更新时间）
- 百度站长平台提交站点 + 手动提交 sitemap 链接，等收录
- 顺手在 `post.html` 的 `<title>` 用文章标题替代固定文案（现在 title 是死的「文章 · xiaoxu & xiaofu」，SEO 大忌）

#### ④ 访问统计（自研轻量，2 小时）

**方案**：不引 GA/百度统计（国内加载慢 + 隐私顾虑），自研计数：
- `store.js` 加 `views` 表：`(page, date, cnt)` + `page_views` 表（每篇文章累计）
- `server.js` 在 `serveStatic`/`handleAPI` 入口记一笔（内存计数 + 每 60s 落库，减少写放大）
- **只在后台「数据与安全」页展示**：今日 PV / 总访问 / 近 7 天趋势 / 热帖 TOP5（前台不加任何统计痕迹）
- 后台「数据与安全」页展示近 7 天趋势（简单 ASCII/表格即可）

> 统计的意义不是数字好看，是**回答「该往哪使劲」**：城市地图访问高 → 就给它做海报武器；博客没人看 → 换选题。

---

### 🚀 P1 · 本月（增长武器）

| # | 项目 | 为什么做 | 工作量 | 验收标准 |
|---|---|---|---|---|
| ⑥ | 恋爱海报生成器 | 把城市地图/纪念日变成分享核弹 | 1.5 天 | 一键生成海报图+二维码，可下载 |
| ⑦ | RSS 订阅 | 博客读者能订阅，沉淀回头客 | 2 小时 | `/rss.xml` 可订阅 |
| ⑧ | 站内搜索 | 数据量小，前端就能做 | 2 小时 | 顶部搜索框全站搜文章/项目 |

#### ⑤ 恋爱海报生成器（城市地图变现）

**现状**：`cities.html` + `assets/boundaries/*.json` 已有完整中国地图数据——国内个人站的稀缺货，但只在自己站点里吃灰。

**方案**：
- 新页面 `poster.html`（或城市页内嵌）：
  - 输入/勾选：走过城市、在一起天数、纪念日、一句话
  - 前端用 Canvas 绘制海报（背景用暖橙渐变 + 城市点亮 + 天数大字 + 二维码）
  - 二维码指向 `https://www.xiaoxu-xiaofu.top`（零依赖可用 `canvas` 手绘 or 用现成库，若坚持零依赖：画一个简化定位码样式即可）
  - 一键下载 PNG（`canvas.toDataURL`）+ 一键复制分享文案（「我们走过 N 座城，N 天啦」）
- 水印统一带站名+域名，海报即广告位
- 后台可配置文案模板

**传播逻辑**：朋友圈/小红书晒一张漂亮海报 = 免费给你站引流，还自带"秀恩爱"社交货币，比写文章传播猛 10 倍。

#### ⑥ RSS

`server.js` 加 `/rss.xml` 路由：从 SQLite 取文章列表，拼标准 RSS 2.0 XML（零依赖手写字符串即可，20 行）。在 `blog.html` 顶部加一个 RSS 小图标链接。

#### ⑦ 站内搜索

- `blog.html`/`projects.html` 顶部加输入框
- 前端拉一次 `/api/content` 后本地 `includes()` 过滤（站点数据量小，无需后端索引）
- 结果按「标题 > 摘要 > 正文」权重排序，空态给引导

---

### 🌱 P2 · 随缘（体验深化，不急）

| # | 项目 | 说明 | 工作量 |
|---|---|---|---|
| ⑩ | 语义化 URL | `post.html?id=1` → `/posts/建站小记`（改动大，收益中等，可后置） | 2 天 |
| ⑪ | JSON-LD schema | 文章用 `Article`、站点用 `Person`/`Organization`，利于谷歌/必应结构化展示 | 2 小时 |
| ⑫ | PWA | manifest + service worker，可"添加到主屏幕"当 App 用 | 0.5 天 |
| ⑬ | 年度报告长图 | 年底自动生成「我们这一年」数据长图（呼应海报生成器） | 2 天 |
| ⑭ | 即刻/小红书双人账号互挂 | 内容分发阵地，站内海报自带站名水印，反哺回流 | 运营向 |

---

## 4. 设计增强（技术感改造）

> 现状盘点：粒子背景、卡片聚光灯、3D 倾斜、磁性按钮、打字机、数字滚动、reveal 淡入**都有**。
> 但都是「点缀型」动效，缺「系统型」技术感——没有页面转场、没有滚动驱动反馈、没有光标系统、没有终端/数据感的工程点缀。技术感强的站（Linear / Vercel / 即刻）靠的就是这几样，且**全部零依赖、渐进增强**，不破坏现有架构。

### ⚡ QW 快速见效（合计 0.5~1 天）

| # | 项目 | 实现要点 | 工作量 |
|---|---|---|---|
| Q1 | **View Transitions 页面转场** | 原生 `document.startViewTransition()`（Chrome/Edge/Safari 18+ 支持，零依赖）；`main.js` 劫持导航点击渐进增强；CSS 给 `.logo`/`.site-header`/卡片加 `view-transition-name`，整站从「啪一下刷新」变「丝滑换页」；老浏览器自动降级为普通跳转 | 2h |
| Q2 | **自定义光标系统** | 小圆点紧跟 + 大外环缓动拖尾（rAF 循环 ~60 行）；hover 链接/按钮/卡片时外环放大并显示 `OPEN ↗`；`@media (pointer: coarse)` 触屏设备自动禁用 | 2h |
| Q3 | **顶部滚动进度条** | 2~3px 渐变细条，按页面滚动进度填充；纯 CSS transform + 一个 scroll 监听 | 0.5h |
| Q4 | **Hero 入场动画序列** | hero 文本/按钮交错 fade-up：CSS 变量 `--d` 控制逐项延迟（`0.1s → 0.6s`），与现有 reveal 不冲突 | 0.5h |
| Q5 | **终端状态块** | 等宽字体（`ui-monospace`）小终端块点缀：`> uptime: 628d 07:12` + 闪烁光标；统计上线后可联动显示「访客总数」，技术感 + 数据感双杀 | 1h |

### 🚀 进阶（合计 1~2 天，随缘档）

| # | 项目 | 实现要点 |
|---|---|---|
| Q6 | 滚动视差 | `data-parallax` 属性 + rAF，hero/卡片随滚动轻微位移，分层感 |
| Q7 | 等宽字体排版点缀 | 文章/项目编号用 mono（`01 / 02`）、时间戳风格、代码块加扫描线纹理 |
| Q8 | 网格/噪点纹理 | hero 背景淡网格 + 卡片 hover 显示扫描线，增加「工程感」 |
| Q9 | 描边大数字 | `// 01 · LATEST POSTS` 区段号升级为滚动驱动的描边字/数字跳动 |

**铁律**：所有动效必须遵循 `prefers-reduced-motion: reduce` 直接降级（CSS 已有雏形，补全覆盖全部新动效）；禁用触屏设备上的光标类效果；不引任何依赖库。

---

## 5. 内容运营指南（写什么更受欢迎）

已有内容验证过什么有效：**《建站小记》《桌面工作流进化史》是典型的"折腾记录"，是程序员内容里传播性最强的品类。** 往下走：

1. **连续剧式栏目**：每周固定一篇「周末去哪 / 折腾日志」，有规律才有订阅感（RSS 上线后尤其重要）
2. **双人反差内容**：同一个事件，他写代码视角、她画插画视角——情侣账号的反差内容传播性极强
3. **数据型内容**：纪念日、城市、照片墙做成可视化（你已有"628 天在一起"计时器，把它升级成月度/年度数据卡）
4. **选题优先级**：折腾记录 > 旅行/探店 > 双人日常 > 感悟随笔
5. **发布节奏**：宁每周 1 篇高质量，不憋大招月更 1 篇

---

## 6. 风险与注意事项

| 风险 | 说明 | 对策 |
|---|---|---|
| 备案 | 大陆服务器绑域名必须 ICP 备案（README 已提及） | 确认 xiaoxu-xiaofu.top 备案状态，未备案则用 47.103.27.76:3000 裸 IP 分享受限 |
| 评论/留言滥用 | 垃圾评论、XSS | 复用现有限频+蜜罐+转义；评论 200 条/文章上限；后台可删 |
| 上传安全 | `data/uploads/` 无类型白名单风险 | 现有 `/api/upload` 校验 base64；建议加扩展名/大小白名单（图片格式 + ≤5MB） |
| SQLite 并发 | WAL 模式已开，单人站无压力 | 保持事务写入，计数落库用批处理（60s 合并） |
| 零依赖边界 | 海报生成器/二维码若引库会破洁癖 | Canvas 方案可零依赖；二维码若必须，给一个最小实现或接受引入单文件库 |
| HTTPS | 裸 IP 是 http，微信抓 og:image 可能要求 https | 建议域名上 HTTPS（服务器已配 nginx，证书现成的可续） |

---

## 7. 执行清单（勾选版）

### P0（本周）
- [ ] ① 文章级评论：store.js 建表 → server.js 两路由 → site.js 渲染 → admin 管理
- [ ] ② og:image：后台加默认分享图 → 全站 meta 注入 → 微信实测
- [ ] ③ robots.txt + sitemap.xml + 百度提交 + post title 动态化
- [ ] ④ 访问统计：views 表 + 入口计数 + 后台展示（前台零痕迹）
- [ ] ⑤ 技术感 QW 包：Q1 页面转场 + Q2 光标系统 + Q3 滚动进度条 + Q4 Hero 入场动画 + Q5 终端状态块

### P1（本月）
- [ ] ⑥ 恋爱海报生成器（城市地图 + 纪念日 + 二维码 + 下载）
- [ ] ⑦ rss.xml + blog 页订阅入口
- [ ] ⑧ 站内搜索
- [ ] ⑨ 技术感进阶：Q6 滚动视差 + Q7 mono 排版 + Q8 网格/扫描线纹理

### P2（随缘）
- [ ] ⑩ 语义化 URL
- [ ] ⑪ JSON-LD
- [ ] ⑫ PWA
- [ ] ⑬ 年度报告长图
- [ ] ⑭ 社交账号互挂

---

## 8. 预期效果

完成 P0 后（约 1 周）：访客能从微信/小红书点进漂亮卡片 → 在文章下留言 → 你从统计里看到什么内容受欢迎 → 顺着数据做 P1 海报武器 → 海报再带回新访客。**小闭环跑起来，站点就"活"了。**
